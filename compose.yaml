services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=loja_virtual_mentoria'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'

    expose:
      - '5432'
